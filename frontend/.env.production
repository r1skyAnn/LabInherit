# 生产环境 API 配置
# 前端打包后没有 Vite proxy，必须指定完整的后端地址

# 开发/本地部署：使用 localhost
VITE_API_BASE=/api/v1
VITE_BACKEND_URL=http://localhost:8000

# 生产部署示例（根据你的服务器地址修改）:
# VITE_API_BASE=http://你的服务器IP:8000/api/v1
# VITE_BACKEND_URL=http://你的服务器IP:8000
