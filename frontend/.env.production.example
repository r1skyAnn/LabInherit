# Frontend production environment variables
# Copy this to .env.production and modify

# API base path (usually /api/v1 if served from same domain)
VITE_API_BASE=/api/v1

# Production build optimizations
NODE_ENV=production
